---
# tasks/network_tools.yml - Install network diagnostic tools

- name: Install network diagnostic tools
  ansible.builtin.apt:
    pkg:
      - nmap
      - tcpdump
      - netcat-openbsd
      - hping3
      - arp-scan
      - masscan
      - mtr
      - iperf3
      - bmon
      - iftop
      - nethogs
      - vnstat
      - dnsutils
      - whois
      - traceroute
      - net-tools
      - iproute2
      - iputils-ping
      - socat
      - telnet
      - smbclient
      - snmp
      - ldap-utils
      - wget
    state: present
  become: true

- name: Install wireless tools (best-effort; not all are in every repo)
  ansible.builtin.apt:
    pkg: "{{ item }}"
    state: present
  become: true
  loop:
    - aircrack-ng
    - kismet
    - wavemon
    - wireless-tools
  when: ansible_facts['os_family'] == "Debian"
  register: _wireless_pkg_install
  failed_when:
    - _wireless_pkg_install is failed
    - '"Unable to locate package" not in (_wireless_pkg_install.msg | default(""))'
    - '"No package matching" not in (_wireless_pkg_install.msg | default(""))'
