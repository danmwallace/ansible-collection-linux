---
driver:
  name: default
  options:
    managed: true
platforms:
  - name: mikrotik-backup-instance
provisioner:
  name: ansible
  inventory:
    hosts:
      molecule:
        hosts:
          mikrotik-backup-instance:
            ansible_connection: containers.podman.podman
            ansible_python_interpreter: /usr/bin/python3
scenario:
  test_sequence:
    - dependency
    - destroy
    - create
    - prepare
    - converge
    - idempotence
    - verify
    - destroy
